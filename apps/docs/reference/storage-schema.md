# Storage Schema

The generated storage schema page is built from a real migrated in-memory database, so it reflects the actual current SQLite layout instead of a hand-maintained draft.

- [Generated Storage Schema](./generated/storage-schema)

Use it for table names, column definitions, indexes, and migration inventory.
