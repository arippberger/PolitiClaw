{
  "name": "politiclaw-workspace",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @politiclaw/politiclaw && npm run build --workspace @politiclaw/docs",
    "dev:plugin": "npm run dev --workspace @politiclaw/politiclaw",
    "docs:dev": "npm run dev --workspace @politiclaw/docs",
    "docs:generate": "npm run docs:generate --workspace @politiclaw/politiclaw",
    "docs:check": "npm run docs:check --workspace @politiclaw/politiclaw",
    "docs:build": "npm run build --workspace @politiclaw/docs",
    "docs:preview": "npm run preview --workspace @politiclaw/docs",
    "typecheck": "npm run typecheck --workspace @politiclaw/politiclaw",
    "lint": "npm run lint --workspace @politiclaw/politiclaw",
    "test": "npm run test --workspace @politiclaw/politiclaw",
    "release:check": "npm run docs:check && npm run typecheck && npm test && npm run build && npm run smoke:runtime --workspace @politiclaw/politiclaw && npm run pack:check --workspace @politiclaw/politiclaw"
  },
  "engines": {
    "node": ">=20"
  }
}
