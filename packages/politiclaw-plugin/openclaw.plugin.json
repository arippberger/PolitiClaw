{
  "id": "politiclaw",
  "name": "PolitiClaw",
  "description": "Local-first civic copilot that holds your representatives accountable to the values you declare. Learns the stances you care about, watches federal legislation and elections for you, and flags when your reps' votes and actions align — or don't — with those stances. Drafts letters you send yourself; never speaks on your behalf and never tells you how to vote.",
  "version": "0.0.11",
  "skills": ["skills"],
  "commandAliases": [
    { "name": "politiclaw-help", "kind": "runtime-slash" },
    { "name": "politiclaw-setup", "kind": "runtime-slash" },
    { "name": "politiclaw-status", "kind": "runtime-slash" },
    { "name": "politiclaw-doctor", "kind": "runtime-slash" },
    { "name": "politiclaw-keys", "kind": "runtime-slash" },
    { "name": "politiclaw-version", "kind": "runtime-slash" }
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKeys": {
        "type": "object",
        "additionalProperties": false,
        "description": "One key required (apiDataGov); all others are optional upgrades. The required key powers federal bills, House roll-call votes, committee schedules, and FEC finance data. Senate roll-call votes ingest separately through voteview.com (zero-key, no signup).",
        "properties": {
          "apiDataGov": {
            "type": "string",
            "description": "REQUIRED. Shared api.data.gov key used by both api.congress.gov and FEC OpenFEC. One key covers both. Free, instant signup at https://api.data.gov/signup/."
          },
          "geocodio": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. Reps-by-address via API. Default path uses the zero-key local shapefile pipeline; Geocodio trades disk footprint for API simplicity. Free tier 2500 lookups/day."
          },
          "openStates": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. State bills and votes with individual member positions. Without a key, state bill lookup is narrative-only via LLM search; state vote positions and state change-detection require this."
          },
          "legiscan": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. State bills fallback or primary source. Free tier 30,000 queries/month. Covers federal and state in one key if the user prefers unified access."
          },
          "openSecrets": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. Federal campaign-finance derived analytics such as industry rollups and revolving-door context. Non-commercial use only. Without a key, narrative context falls back to LLM search, but never for dollar amounts."
          },
          "followTheMoney": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. State-level campaign finance. Without this, state finance data is explicitly not covered."
          },
          "voteSmart": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. Candidate bios for ballot explanations. Default bios come from LLM search and are tagged as low-confidence narrative; Vote Smart provides structured, verified bios."
          },
          "democracyWorks": {
            "type": "string",
            "description": "OPTIONAL UPGRADE. Ballot logistics such as dates, deadlines, and polling places. Partner-gated; requires an application. Default uses Google Civic voterInfoQuery (free, less reliable)."
          },
          "cicero": {
            "type": "string",
            "description": "OPTIONAL UPGRADE (paid). Local municipal, county, and school-board representative coverage. This is the only local source; without it, local reps are explicitly not covered."
          },
          "ballotReady": {
            "type": "string",
            "description": "OPTIONAL UPGRADE (commercial). Structured down-ballot data. Default scope covers federal, statewide, and six secretary-of-state feeds; BallotReady lights up fuller down-ballot coverage."
          },
          "googleCivic": {
            "type": "string",
            "description": "OPTIONAL but required for politiclaw_get_my_ballot. Google Cloud API key with the Civic Information API enabled for voterInfoQuery. Distinct from api.data.gov; create it in the Google Cloud console."
          }
        }
      },
      "sources": {
        "type": "object",
        "additionalProperties": false,
        "description": "Per-source configuration. Keys here are independent of apiKeys; they control adapter selection and base URLs for self-hosted mirrors.",
        "properties": {
          "bills": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "scraperBaseUrl": {
                "type": "string",
                "description": "OPTIONAL. Base URL for a self-hosted unitedstates/congress scraper output mirror. When set, bill lookups fall back to this mirror if apiDataGov is absent or rate-limited. Adapter expects the canonical layout: {baseUrl}/{congress}/bills/{billtype}/{billtype}{number}/data.json. No default — the scraper is designed to be self-hosted and we do not endorse any third-party mirror."
              }
            }
          }
        }
      }
    }
  }
}
